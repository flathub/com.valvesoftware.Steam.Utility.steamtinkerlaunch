name: pev
no-autogen: true
buildsystem: simple
build-options:
  cppflags: -DPLUGINSDIR=/lib/pev/plugins/
build-commands:
  - make prefix=${FLATPAK_DEST}
  - make prefix=${FLATPAK_DEST} install
sources:
  - type: git
    url: https://github.com/mentebinaria/readpe/
    tag: v0.82
    commit: ad4d5a586f022f36b6b925a63ffd667cb729731d
    x-checker-data:
      type: git
      tag-pattern: ^v([\d.]+)$
