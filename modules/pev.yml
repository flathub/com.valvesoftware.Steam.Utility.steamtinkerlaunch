name: pev
no-autogen: true
buildsystem: simple
build-options:
  cppflags: -DPLUGINSDIR=/lib/pev/plugins/
build-commands:
  - make prefix=${FLATPAK_DEST}
  - make prefix=${FLATPAK_DEST} install
sources:
  - type: git
    url: https://github.com/mentebinaria/readpe/
    tag: v0.83
    commit: 4a87bc9fac83a334dd2d6c4f63e9a5172b1d5fc7
    x-checker-data:
      type: git
      tag-pattern: ^v([\d.]+)$
