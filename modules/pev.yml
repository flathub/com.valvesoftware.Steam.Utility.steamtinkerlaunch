name: pev
no-autogen: true
buildsystem: simple
build-options:
  cppflags: -DPLUGINSDIR=/lib/pev/plugins/
build-commands:
  - make prefix=${FLATPAK_DEST}
  - make prefix=${FLATPAK_DEST} install
sources:
  - type: git
    url: https://github.com/mentebinaria/readpe/
    tag: v0.83
    commit: 7f3c13649959747ec57db172da355038c3521e1e
    x-checker-data:
      type: git
      tag-pattern: ^v([\d.]+)$
