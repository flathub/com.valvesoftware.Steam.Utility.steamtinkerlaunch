name: pev
no-autogen: true
buildsystem: simple
build-options:
  cppflags: -DPLUGINSDIR=/lib/pev/plugins/
build-commands:
  - make prefix=${FLATPAK_DEST}
  - make prefix=${FLATPAK_DEST} install
sources:
  - type: git
    url: https://github.com/mentebinaria/readpe/
    tag: v0.84
    commit: 4b88b9a4cf22b74eb808c48d69005db7804dc5a0
    x-checker-data:
      type: git
      tag-pattern: ^v([\d.]+)$
